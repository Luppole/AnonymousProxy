# AnonymousProxy
Super Duper Robust &amp; Secure Local Host Proxy Server

First, run ```python generate_cert.py``` to generate the files:
```cert.pem``` & ```key.pem```

Then run ```python proxy.py``` and enjoy your cool proxy server!
